# UI V2 Visual Parity Review

**Status:** PM/UX final visual review reopened after live preview found shell/sidebar primitive mismatch and later Docs / AI Trace route-composition drift. The latest source-led recovery adds sidebar first-paint primitives, sidebar label-pill/Scope dot parity, interactive desktop Scope filtering, topbar scope-picker chevron/reset behavior, first-paint loading emoji removal, honest disabled states for non-wired desktop filter controls, route-header action usability for Calendar / Boards / Weekly Focus / All Tasks / Docs, Today Next Up eyebrow time-order parity, app-pane/mobile shell breakpoint checks, desktop topbar/Today task-row refinements, Weekly Focus live-data lane composition/card primitive repair, a Docs / AI Trace route rebuild against `ScreenTrace` source, a Settings integration edit-surface UI that stays session-only/write-only for credentials, an All Tasks Edit Card modal design-system repair, a web-app-wide hardcode/non-interactive control recovery pass, a Review / Docs / Today route-header false-affordance follow-up, status-strip hover/focus descriptions, Settings button/chip primitive normalization, topbar dropdown/action UX recovery, a whole-system micro-QC ledger for remaining UX suspects, a fourth-pass fix-detail appendix, a Review Queue duplicate-refresh cleanup, forced state fixture evidence for priority loading/empty/no-match/missing-context/disconnected states, and a small-label muted-token contrast polish for Docs / All Tasks / Settings. The ledger now has 93 rows and includes implementation-level explanations for hidden/zero-size controls, focus return, readonly chips, sort semantics, target sizing, text clipping, evidence/tooling accuracy, duplicate route-action affordances, muted-label readability, and state fixture coverage. Automated QA is passing again; PM/UX visual re-review remains the acceptance gate. This document keeps selector/no-overflow PASS separate from source-led visual parity findings.

**Severity model:**
- `P0`: visual artifact, overlap, blank/black blob, unsafe copy, console/page error, horizontal overflow.
- `P1`: component pattern mismatch, wrong hierarchy, wrong route composition, bad density, missing first-viewport priority.
- `P2`: spacing, tone, casing, chip/icon/line-height mismatch.

## Current Priority Findings

| Area | Prototype Source | PM/UX Finding | Severity | Required Action |
| --- | --- | --- | --- | --- |
| App-pane compact desktop route actions | `components.jsx` `Topbar`; prototype shell/sidebar/status strip; `QC-001` / `UIV2-DEC-001` | PM/UX chose desktop-first behavior for the `747px` app pane. The previous mobile-shell breakpoint hid route actions too early, making Filter/Export/New controls hard to find for split-screen desktop users. | Closed P1 / PM re-review pending | R171 now treats `701-820px` as compact desktop shell: status strip + sidebar remain visible, mobile bottom nav is hidden, and topbar route actions remain reachable. `verify:uiv2-full-fidelity` now guards app-pane compact shell and route-action reachability for Today, Review, All Tasks, Calendar, and Docs. Targeted evidence: `app-pane-route-actions-{today,docs,calendar,all,review}-2026-05-18.png`; targeted QA found overflow `0` and console/page errors `0`. |
| Shared text reveal policy | `screens-aux.jsx` data-readability guidance; `TaskTable`, Calendar event chips, Planner rows, OKR KR rows; `QC-074` / `QC-086` / `UIV2-FIX-005` | PM/UX proceeded with the conservative reveal policy so dense UI V2 rows can handle long Thai/English live data without silently hiding decision-critical words. | Closed P2 / PM re-review pending | R172 adds shared `.uiv2-decision-text` and `.uiv2-meta-text` primitives. Decision-critical text gets two-line clamp plus native reveal; metadata keeps one-line density plus native reveal. Today, All Tasks, Calendar, Planner, and OKR targeted proof found missing reveal count `0`, overflow `0`, and console/page errors `0`. Evidence: `text-reveal-{today,all-tasks,calendar,planner,okr}-2026-05-18.png`. Calendar true-mobile agenda-first behavior remains a separate `UIV2-DEC-004` decision. |
| Settings copy tone / session boundary | `screens-3.jsx` `ScreenSettings`; `IntegrationRow`; `SettingRow`; `QC-008` / `QC-032` / `QC-053` | PM/UX asked for Settings connection/integration controls to feel editable but safe, without intimidating secret/OAuth/token-style wording or implying live runtime changes. | Closed P1/P2 / PM re-review pending | R173 updates Settings visible copy to private-value/session-only language, labels write-only fields as `Private connection value A/B`, and adds a visible Paperclip `Session-only draft. Live Paperclip intake is unchanged.` note with scoped 24px controls. Browser QA found unsafe visible-copy hits `0`, Paperclip Disabled pressed state plus live-mode-unchanged toast, overflow `0`, and console/page errors `0`. Evidence: `settings-copy-tone-session-boundary-2026-05-18.png`. |
| Muted small-label contrast | `prototype/css/tokens.css` `.eyebrow`; `components.jsx` trace/table/settings primitives; `QC-069` | Third-pass QC found small uppercase labels could read too faintly on light surfaces, especially Docs trace headers, All Tasks headers, and Settings section labels. | Closed P2 / PM re-review pending | R174 keeps the restrained UI V2 tone but adds production token aliases for prototype type (`--t-eyebrow`, `--t-mono`, `--t-mono-sm`) and a scoped `--muted-label` color for small operational labels. Browser QA on `/docs` found trace headers at contrast `6.82:1`; targeted Playwright found Settings labels at `6.82:1`; controlled full-fidelity fixtures guard All Tasks populated table headers. Evidence: `small-label-contrast-docs-2026-05-19.png`, `small-label-contrast-settings-2026-05-19.png`, and regenerated full-fidelity audit evidence. |
| Review Queue duplicate refresh affordance | `components.jsx` `Topbar`; `screens-1.jsx` `ScreenReview`; QC-093 | PM/UX live Review Queue review found two refresh controls in the same topbar: the global refresh icon and a route-level `Refresh` button. This made command hierarchy feel redundant and less professional. | Closed P2 / PM re-review pending | Review Queue route actions now keep `Filter` only and use the global topbar refresh icon as the canonical refresh control. `verify:uiv2-full-fidelity` now fails if `#review-topbar-refresh` returns or if the global refresh is missing. Browser QA confirmed visible refresh count `1`, Filter focuses the Review filterbar, overflow `0`, and console warnings/errors `0`. Evidence: `review-topbar-refresh-dedup-2026-05-18.png`. |
| Forced state fixture evidence expansion | `screens-aux.jsx` state acceptance notes; `StateCard`, loading, empty, no-results, disconnected route surfaces; QC-005 / QC-014 / QC-015 / `UIV2-QA-001` | Whole-system QC previously depended heavily on live populated preview, so state-specific UI regressions could survive when production data did not naturally expose loading, empty, filtered no-match, Review safety guard, or disconnected states. | Closed P1/P2 evidence gap / PM re-review pending | R154/R155 adds `verify:uiv2-state-fixtures` plus controlled desktop screenshots for Today loading, Review Queue empty, Review Queue filtered no-match, Review Queue missing-context approval guard, All Tasks filtered no-match, Docs / AI Trace filtered no-match, Calendar disconnected, and Planner disconnected. Evidence: `docs/logs/screenshots/v06-uiv2-state-fixtures/` with `uiv2-state-fixture-results.json` and `uiv2-state-fixture-summary.md`. This is evidence expansion only; broader route-specific error/mobile state work remains separate. |
| Whole-system UX micro-QC ledger | `screens-aux.jsx` acceptance notes; prototype `Topbar`, `FilterBar`, `StateCard`, `TaskTable`, `IntegrationRow`, mobile shell, and route screen sources | PM/UX requested a fresh QC pass that does not let small route/control/state issues slip through just because automated route coverage passes. The ledger now records 93 route/section/control/state rows, including app-pane action visibility, mobile hamburger decision, popup focus-return proof, state coverage gaps, Docs mobile trace clipping, Settings input labels/copy review, Calendar `New event` scope, Today Filter model, evidence/tooling gaps, Docs mobile filterbar collapse/overlap, Calendar mobile unreadable event chips, All Tasks small table hit targets, topbar Scope focus-ring gap, empty mobile toast shell, Weekly Focus mobile lane priority, status-strip keyboard-tab cost, unlabeled/no-ring route search fields, hidden legacy modal shells, hidden zero-size global controls, body focus dumps, readonly chip semantics, missing button types, cursor/hit-area inconsistency, All Tasks sort `aria-sort` gap, repeated generic row/action labels, low-contrast muted labels, route heading semantics, long live-data clipping policy, and duplicate route-action affordances. | Open P1/P2/P3 triage | Use `UI_V2_WHOLE_SYSTEM_UX_QC_LEDGER.md` and `UI_V2_WHOLE_SYSTEM_UX_QC_FIX_DETAIL_APPENDIX.md` as the next PM/UX triage queue. No product code/runtime change was made in these QC passes. P1/P2 rows must be fixed, explicitly accepted, or deferred before claiming final UI/UX acceptance. Evidence is under `docs/logs/screenshots/v06-uiv2-whole-system-qc/` and targeted route screenshots under `docs/logs/screenshots/v06-uiv2-full-fidelity/`. |
| Empty toast shell / mobile bottom-nav artifact | `screens-mobile.jsx` mobile shell; shared `toast()` feedback helper; QC-055 | QC-055 found the empty fixed `#toast.toast` shell near the mobile bottom nav, and the first visibility fix revealed populated toasts could still sit over the `.mobile-route-bar`. | Closed P2 / PM re-review pending | Toast now starts `aria-hidden=true`, `visibility:hidden`, with no text. `toast()` flips `aria-hidden=false` only while populated, then hides and clears content after timeout. Mobile viewports offset toast above the bottom route bar, preserving an 18px gap in `375x667` and `390x844` checks with overflow `0` and no console warnings/errors. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/toast-empty-hidden-mobile-small-2026-05-18.png`, `toast-visible-mobile-small-2026-05-18.png`, `toast-empty-hidden-mobile-2026-05-18.png`, and `toast-visible-mobile-2026-05-18.png`. |
| Hidden-control and semantic blocker reconciliation | `UIV2-FIX-001`; `UIV2-FIX-003`; `openSurface`, `closeSurface`, `syncClosedSurfaces`; All Tasks `TaskTable` | Follow-up audit found that broad hidden-control scanners can still report zero-size descendants after R118 if they do not consider hidden/inert ancestors. | Closed P1/P2 / PM re-review pending | Fresh ancestor-aware `/all` audit across desktop, app-pane, and mobile-small confirmed active zero-size focusables `0`, missing visible button type count `0`, and overflow `0`. Closed modals and the legacy sidebar boards section are hidden by `aria-hidden=true` / `inert`; responsive task-check descendants under hidden/inert table ancestors are not active controls. `QC-063`, `QC-076`, `QC-081`, `QC-082`, and `QC-087` now point to PM/UX review pending rather than open blockers. |
| Icon label / Settings label / Scope semantics reconciliation | `components.jsx` `Topbar`, `TaskTable`, `IntegrationRow`; `screens-3.jsx` `ScreenSettings`; `UIV2-FIX-003`; `UIV2-FIX-008` | Ledger rows `QC-007`, `QC-016`, `QC-033`, and `QC-079` remained marked open even though later implementation passes had already repaired the underlying semantics. | Closed P2 / PM re-review pending | R140 DOM audit on `/all`, `/settings`, and `/docs` found unnamed visible icon-only buttons `0`, visible unnamed editable fields `0`, `.scope-pick` rendered as native `button[type=button]` with `aria-haspopup=listbox` / `aria-expanded=false`, and overflow `0`. Rows now point to PM/UX review pending rather than open blockers. |
| All Tasks table header affordance | `screens-1.jsx` `TaskTable`; `screens-aux.jsx` dense table interaction notes; QC-072 / QC-081 | QC-072 found the dense All Tasks header technically worked but was visually too muted and tiny for a repeated desktop work surface. | Closed P2 / PM re-review pending | All Tasks table headers now keep compact UI V2 density while improving affordance: 11px / 700 text, darker header tone, 28px hit surface, full-width hover/focus target, cobalt hover state, and verifier coverage for font/weight/color plus existing `aria-sort` behavior. Browser QA confirmed Due click changes visible arrow and `aria-sort`, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/all-tasks-header-affordance-2026-05-18.png`. |
| Docs desktop trace identity readability | `screens-3.jsx` `ScreenTrace`; trace table / evidence card governance pattern; QC-071 | QC-071 found desktop Docs trace rows could still hide important run IDs and long agent names even after mobile audit-card repairs. | Closed P2 / PM re-review pending | Desktop trace rows now allow run IDs to wrap inside the trace capsule while retaining the copy affordance, and title/agent cells expose `title` fallback plus two-line readable treatment. Browser QA confirmed the first two rows show full run IDs and agent names without horizontal overflow, copy buttons remain present, and console warnings/errors are `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-trace-desktop-identity-readability-2026-05-18.png`. |
| All Tasks long-text reveal path | `screens-1.jsx` `TaskTable`; `screens-aux.jsx` density/readability notes; QC-085 | QC-085 found long board tags, next-action strings, and task/OKR titles could clip in dense All Tasks rows without an obvious reveal path. | Closed P2 / PM re-review pending | Dense row density is preserved, but decision-critical row, task title, next-action, board/list, and list-name surfaces now expose `title` fallback. Browser QA found 12 checked rows, missing reveal count `0`, three clipped titles with reveal present, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/all-tasks-long-text-reveal-2026-05-18.png`. |
| Route landmark semantics | `components.jsx` shell/topbar composition; UI V2 topbar route title; QC-070 | QC-070 found the app-level `h1` repeated the brand on every route while the actual route title lived in the topbar, which made page changes less explicit for assistive technology. | Closed P2 / PM re-review pending | The visible prototype hierarchy is preserved, but the shell now renders the route content as `<main id="app-main" aria-labelledby="board-title">`. Browser QA on `3032/today` confirmed the main landmark is labelled by the current route title `Today`, the global brand heading remains unchanged, overflow is `0`, and console warnings/errors are `0`. `verify:uiv2-full-fidelity` now requires the route landmark contract. |
| Today / Planner / OKR long-text reveal path | `screens-1.jsx` Today dense work rows; `screens-2.jsx` Planner source columns; `screens-3.jsx` OKR progress rows; QC-050 / QC-051 / QC-052 | Long Thai/English live titles could clip in Today work rows, Planner deadline rows, and OKR KR rows without a reliable reveal path. | Closed P2 / PM re-review pending | The compact prototype density is preserved, but Today rows, mobile Today rows, Planner Google/Trello task rows, shared board tags, and OKR KR rows now expose native reveal coverage for full task title, board/list/source metadata, and KR names. R172 extends this into the shared two-line decision-text policy under `QC-074`; controlled `verify:uiv2-full-fidelity` guards the populated row contracts. |
| Readonly/static chip truthfulness | `screens-2.jsx` Boards Monitor; `screens-3.jsx` OKR / Portfolio and Settings; `screens-aux.jsx` chip/status guidance; QC-006 / QC-019 / QC-025 | Whole-system scanner flagged owner/status chips that might look clickable without behavior. Source-led review showed several were already static metadata, but there was no verifier guard separating metadata from real filters. | Closed P2 / PM re-review pending | `verify:uiv2-full-fidelity` now requires Boards owner metadata to stay static text, Boards planned-filter chips to expose disabled explanation titles, OKR owner metadata to stay out of button/filter-chip primitives, and Settings static status chips to remain non-button elements while segment buttons expose state. Live Browser spot-check on `3032/boards`, `3032/okr`, and `3032/settings` found fake metadata controls `0`, bad segment semantics `0`, overflow `0`, and console warnings/errors `0`; controlled fixtures provide populated-row proof. |
| Boards warning mode evidence | `screens-2.jsx` `ScreenBoards`; Boards segmented mode controls; QC-020 | The QC ledger still lacked current before/after proof that Convention warnings visibly changes Boards state and explains the metadata-warning mode. | Closed P2 / PM re-review pending | Boards mode controls now expose `aria-pressed` and safe titles for Board mode, Team mode, and Convention warnings. Browser QA confirmed Convention warnings toggles selected state, keeps overflow `0`, and shows naming/owner convention context without implying Trello writes. Evidence: `boards-warning-mode-before-2026-05-18.png` and `boards-warning-mode-after-2026-05-18.png`. |
| Planner Google Tasks add-control gating | `screens-2.jsx` `ScreenPlanner`; Planner source card / disconnected state; QC-023 | Planner mixes Trello deadline planning with Google Tasks connection state, so disconnected Google Tasks must not display add-task controls that imply live writes. | Closed P1 / PM re-review pending | `verify:uiv2-full-fidelity` now requires the disconnected Google Tasks panel to hide `.planner-add-row`, `#planner-add-input`, and `.planner-add-btn`, while showing the connect card and copy that no tasks are written until connected / read-only boundary is clear. Controlled Planner screenshots were regenerated under `docs/logs/screenshots/v06-uiv2-full-fidelity/`; live Browser spot-check found no add controls, overflow `0`, and console warnings/errors `0`. |
| Docs export / rollback action clarity | `screens-3.jsx` `ScreenTrace`; Docs command actions; QC-031 | Docs actions were implemented, but the latest QC ledger still treated export/rollback proof as stale because download feedback and safe rollback wording were not guarded in the current full-fidelity verifier. | Closed P2 / PM re-review pending | Topbar Export now declares local CSV/no-external-system behavior, Copy audit report declares local no-write behavior, and Rollback hint declares guidance-only behavior. `verify:uiv2-full-fidelity` now exercises the Docs export and rollback controls and requires safe visible feedback. Live Browser `/docs` confirmed export feedback, rollback guidance, row select-only behavior, overflow `0`, and console warnings/errors `0`. |
| User-friendly improvement opportunities | Prototype route/source inventory plus PM/UX live-review feedback across global shell, Today, Review Queue, All Tasks, Boards, Calendar, Planner, OKR, Weekly Focus, Docs / AI Trace, Settings, mobile/app-pane, and QA process | PM/UX asked to record not only problems but also all possible improvements that could make the app more convenient and user friendly, including hard/high-effort future improvements. The new opportunity ledger records 60 non-blocking options such as command palette, active context strip, saved views, app-pane action overflow, guided Review Queue checklist, All Tasks presets/detail drawer, agenda-first Calendar, Planner day buckets, OKR detail rail, Weekly Focus wizard, Docs trace detail card, Settings connection wizards, and recurring opportunity review. | Non-blocking opportunity backlog | Use `UI_V2_USER_FRIENDLY_IMPROVEMENT_OPPORTUNITY_LEDGER.md` as PM/UX option bank. These rows are not defects and do not authorize implementation by themselves. PM must choose `Do soon`, `Next iteration`, `Later`, or `Decline` before Dev opens any row. |
| Docs detail reader panel | `UF-050`; `screens-3.jsx` `ScreenTrace`; existing Docs markdown / linked task / evidence / audit renderers | PM/UX requested a full document reader per task from Docs / AI Trace: click a trace row and inspect Markdown, linked task, evidence, audit timeline, and Open Review in one panel. | Closed P2 / PM re-review pending | Trace rows now open a right-side Docs reader drawer while the route keeps the prototype trace table and evidence card as first surfaces. The drawer includes full Markdown, linked Review Queue task context, source evidence, source trace metadata, audit timeline, Copy audit report, Close/Escape focus recovery, and Open Review. Browser QA confirmed the drawer opens from trace rows, Escape returns focus to the row, Open Review navigates to Review Queue, overflow is `0`, and console warnings/errors are `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-reader-detail-desktop-app-pane.png`. |
| Docs reader panel polish | `UF-050`; PM/UX live screenshot review | PM/UX found the first reader pass still had several UX issues: side cards could clip, the drawer felt narrow for a document reader, metadata was dense, and `Open Review` was ambiguous when the linked task was missing locally. | Closed P2 / PM re-review pending | Reader width now expands as a document surface, metadata is labelled as Run / Status / Received / Agent chips, a reader-only no-side-effect note is visible, side cards no longer clip and the side rail scrolls as a column, the close button has clearer UI V2 affordance, and missing local Review links now show `Open Review Queue`. Browser geometry confirmed clipped side-card count `0`, four metadata chips, safety note visible, action copy correct, Escape focus return, Review Queue navigation, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-reader-panel-polish-desktop-app-pane.png`. |
| Docs trace summary / reader role separation | `screens-3.jsx` `ScreenTrace`; PM/UX overlap review | PM/UX asked whether the route evidence card and drawer were overlapping responsibilities. The route card was too close to a second reader because it showed full timeline-style detail directly under the table. | Closed P2 / PM re-review pending | The route card now presents only `Selected trace`, `Trace summary`, source metadata, and a two-event `Audit preview` with an explicit `Open reader` CTA. The drawer is the single full-detail surface for Markdown, linked task, evidence, and complete audit timeline. `verify:uiv2-full-fidelity` now checks this separation and fails if full document content appears inside the summary card. Browser QA confirmed summary/read separation, drawer open from the CTA, overflow `0`, clipped side-card count `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-trace-summary-separated-2026-05-18.png` and `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-summary-reader-separation-2026-05-18.png`. |
| Docs reader content craft and select-first trace rows | `screens-3.jsx` `ScreenTrace`; PM/UX live reader review | PM/UX found the Markdown/evidence content block still looked too raw and said trace-row clicks should not open the reader immediately. | Closed P1 / PM re-review pending | Trace rows now select the trace and update the summary card only; `Open reader` is the explicit drawer-opening control. Reader content now sits on a dedicated document surface with a subtle safety callout, bordered Markdown card, improved heading/list spacing, and a separate evidence card. `verify:uiv2-full-fidelity` now checks that trace rows are select-only and do not auto-open the reader. Browser QA confirmed row-click selection, selected-trace reader opening, overflow `0`, clipped side-card count `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-reader-professional-polish-weekly-2026-05-18.png`. |
| Weekly Focus week/owner controls | `screens-3.jsx` `ScreenFocus`; Weekly Focus route actions; QC-028 | The QC ledger lacked current screenshot proof for Last/This/Next and Owner feedback on the desktop Weekly Focus work surface. | Closed P2 / PM re-review pending | Week controls now expose title help and selected-state guards. Browser QA confirmed This week renders `Week of 18 - 24 May 2026`, Last week changes the route range to `Week of 11 - 17 May 2026`, and Owner gives an honest fallback when no owner filter exists in live data. Evidence: `weekly-focus-this-week-controls-2026-05-18.png` and `weekly-focus-last-week-controls-2026-05-18.png`. |
| Topbar dropdown/action UX | `components.jsx` `Topbar`; `prototype/css/shell.css` `.scope-pick`; `screens-aux.jsx` `Topbar / RouteBar`, `FilterBar` | PM/UX live `/docs` review found topbar controls looked like dropdown/actions but did not provide enough interaction feedback: Scope did not open a route-local menu, Docs Filter only focused the search surface, and topbar popovers needed clearer dismissal/focus behavior. | Closed P1 / PM re-review pending | Scope picker now opens a real listbox popover with All BUs, configured BU rows, active checkmark, disabled unmapped groups, outside-click/Escape dismissal, and aria-expanded state. Docs topbar Filter now opens a compact filter menu for search/status/link/sort with Apply, Show bar, Clear, active count, and no runtime data changes. Bell and Scope popovers mutually dismiss. `restoreFocus()` now adds a short visual `uiv2-focus-restored` confirmation so returned keyboard focus is visible, and `verify:uiv2-full-fidelity` fails if desktop Scope, Bell, or Docs Filter Escape close paths do not return focus to the trigger. Browser QA on `3032/docs` confirmed trigger focus return, visible Scope ring, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/topbar-focus-recovery-docs-2026-05-18.png`. |
| Topbar popup focus recovery expansion | `components.jsx` `Topbar`; Docs route `FilterBar`; QC-003 | The earlier topbar fix covered Escape focus return, but the QC ledger still lacked outside-click proof for Scope, Bell, and Docs Filter. | Closed P2 / PM re-review pending | `verify:uiv2-full-fidelity` now checks Escape and outside-click close paths for Scope, Bell, and Docs Filter. Browser QA confirmed all three popovers open, close, reset `aria-expanded=false`, and return focus to the trigger with overflow `0` and console warnings/errors `0`. Evidence: `topbar-popup-focus-return-docs-2026-05-18.png`. |
| Settings button/chip typography and primitive drift | `screens-3.jsx` `ScreenSettings`; `screens-aux.jsx` `IntegrationRow`, `SettingRow`; `prototype/css/shell.css` `.btn`, `.chip`, `.intg-row` | PM/UX live Settings review found several controls still looked like older production UI: group board pills were heavy blue chips, Delete / Visible / Manage / Policy buttons had inconsistent font weight, radius, and icon treatment, and monitor label chips used a different close-button style. | Closed P2 / PM re-review pending | Settings controls now share UI V2 button/chip rhythm: Onest button font, 26-28px compact heights, 6px radius, restrained brand/ok/danger surfaces, icon+text action buttons, SVG close/delete affordances, and scoped overrides for visible-board rows, BU group chips, integration action buttons, monitor-label chips, and the manage modal. Browser audit on `3032/settings` confirmed Onest fonts, no monospace buttons, action icons present, overflow clean, and no console warnings/errors. |
| Settings Audit / Notifications evidence | `screens-3.jsx` `ScreenSettings`; `SettingRow`; QC-035 / QC-036 | The QC ledger lacked section-anchor proof for Audit / retention and a current notification-toggle interaction matrix. | Closed P2 / PM re-review pending | Audit trace-window controls now expose selected-state/title help and safe Open trace copy. Notification toggles are verifier-guarded as session-only UI drafts. Browser QA confirmed `30d` trace window selection, Weekly focus digest label change to `Monday 09:00`, session-only toast, restore to off, overflow `0`, and console warnings/errors `0`. Evidence: `settings-audit-retention-trace-window-2026-05-18.png` and `settings-notifications-toggle-matrix-2026-05-18.png`. |
| Status strip hover/focus descriptions | `components.jsx` `StatusStrip`; `screens-aux.jsx` `StatusStrip` inventory and telemetry guidance; `prototype/css/shell.css` `.statusbar`, `.sb-group`, `.sb-dot` | PM/UX asked whether each status point could show a description on hover. The prototype status strip already acts as a telemetry edge, but production only showed compact labels/dots, so users had to infer what `dev`, `ui-v2`, `trello`, `gcal`, `paperclip`, `gtasks`, and `last sync` meant. | Closed P2 / PM re-review pending | Desktop status strip groups now expose custom hover/focus tooltips with safe descriptions, keyboard focus support, and native title fallback. Tooltip copy is product-safe and avoids raw `.env`, tokens, secrets, OAuth errors, or runtime-only values. `verify:uiv2-full-fidelity` now fails if status strip descriptions disappear or include unsafe patterns. Live Browser `3032/today` confirmed 8 described items, Trello tooltip visible, overflow `0`, and no console warnings/errors. |
| Route header false-affordance follow-up | `components.jsx` `Topbar`, `RouteBar`, `FilterBar`; `screens-1.jsx` `ScreenReview` / `ScreenToday`; `screens-3.jsx` `ScreenDocsTrace` | Follow-up audit found enabled route-header controls that had onclicks but weak behavior: Review Filter only showed a toast, Review Block intake looked like an operational action without changing route state, Docs Export only showed a handoff toast, and Today Filter focused silently. | Closed P1 / PM re-review pending | Review now has a real `#review-filterbar` with status/risk chips and a no-match state; the routebar action is `Intake policy` and navigates to Settings / Paperclip without runtime change; Docs Export downloads a safe CSV of visible trace rows; Today Filter scrolls/highlights the visible filter segment and explains disabled future filters. `verify:uiv2-full-fidelity` now checks Review policy/filterbar. Live Browser checks confirmed state changes/download feedback and overflow `0`. |
| Edit Card modal visual/system drift | `components.jsx` `Panel` / `StateCard`; `prototype/css/screens.css` `.drawer`, `.drawer-head`, `.drawer-body`, `.drawer-foot`; `tokens.css` form inheritance; `screens-aux.jsx` type/spacing tokens | PM/UX live `/all` review showed the production Edit Card surface still used the old narrow modal: cramped fields, clipped due date, legacy checklist rows, and controls that did not share the UI V2 panel/form rhythm. Prototype source does not define this production modal directly, so the repair uses source form/panel/drawer primitives while preserving the existing Trello edit behavior. | Closed P1 / PM re-review pending | `#card-modal` is now scoped to UI V2 tokens: wider 760px panel, 8px radius, UI V2 header/body/footer rhythm, hidden-text CSS close icon, unclipped two-column date row, product-safe loading/empty checklist states, compact checklist cards, custom checkbox rows, normal checklist text casing, and mobile one-column fallback. Production APIs and save/delete/checklist behavior are unchanged. Live Browser `3032/all` opened a real task modal with overflow `0`, date fields fitting, 5 checklist rows loaded, normal Thai/English checklist casing, and no console warnings/errors. |
| All Tasks visible Edit Card affordance | `screens-1.jsx` `TaskTable`; production Edit Card modal; QC-064 | QC-064 found that All Tasks could open the Edit Card modal from row chrome, but the dense UI V2 table did not show a clear semantic edit/open-card trigger. | Closed P2 / PM re-review pending | All Tasks rows now expose a compact `Edit` button inside the row metadata line, with task-specific accessible names and the existing Edit Card modal as the target. `verify:uiv2-full-fidelity` checks that visible row edit affordances remain present and labelled. Browser QA on `3032/all` confirmed the first Edit button opens the modal, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/all-tasks-visible-edit-affordance-2026-05-18.png`. |
| All Tasks desktop table hit areas | `screens-1.jsx` `TaskTable`; `screens-aux.jsx` density / interaction affordance notes; R126 density policy | QC-043 found sortable headers and row completion controls had 11-14px effective targets even though All Tasks is the team's dense desktop work surface. | Closed P2 / PM re-review pending | The dense table keeps UI V2 compact visuals while separating visual size from hit area: sort buttons expose 28px target height, and row completion controls expose a 28x28 target around the original 14px visual checkbox. `verify:uiv2-full-fidelity` now checks desktop All Tasks table control dimensions. Browser QA on `3032/all` confirmed Due sort still updates `aria-sort`, checkbox art remains 14px, target size is 28px, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/all-tasks-hit-area-repair-2026-05-18.png`. |
| Search field accessibility / focus affordance | `components.jsx` `FilterBar`; `prototype/css/shell.css` search primitives; QC-062 | QC-062 found route search fields could rely on visual placeholder/context and lacked consistent visible focus indication. | Closed P2 / PM re-review pending | All Tasks and Docs / AI Trace search inputs now have explicit accessible names, and `.search-sm` plus sidebar search inputs share the UI V2 cobalt focus ring. `verify:uiv2-full-fidelity` checks All Tasks / Docs search labels. Browser QA confirmed active focus ids, aria labels, visible rings, overflow `0`, and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/search-focus-accessibility-2026-05-18.png`. |
| Contextual repeated-action labels | `components.jsx` `TaskTable`, `IntegrationRow`; `screens-3.jsx` `ScreenOKR`; Planner source cards; QC-065 through QC-068 | Third-pass QC found repeated generic names for All Tasks completion actions, Settings `Manage` / `Visible`, OKR `Open KR detail`, and Planner `Open Settings` CTAs. | Closed P2 / PM re-review pending | Row/action labels now keep the compact UI V2 visible rhythm while exposing task-, integration-, board-, KR-, or source-specific accessible names. `verify:uiv2-full-fidelity` checks All Tasks completion labels, Settings contextual actions, OKR KR labels, and Planner contextual Settings CTAs. Browser QA confirmed OKR KR detail buttons are unique/focusable and Planner Settings CTAs name the Google Tasks / Planner connection context, with overflow `0` and console warnings/errors `0`. Evidence: `docs/logs/screenshots/v06-uiv2-full-fidelity/okr-contextual-kr-detail-labels-2026-05-18.png` and `docs/logs/screenshots/v06-uiv2-full-fidelity/planner-contextual-settings-ctas-2026-05-18.png`. |
| Route header/action usability | `components.jsx` `Topbar`, `RouteBar`, `FilterBar`; `screens-1.jsx` / `screens-2.jsx` / `screens-3.jsx` route action segments | PM/UX live Calendar review found page header controls and several filter chips looked clickable but did not perform a complete action. Known issues included Calendar `Day / Week / Month`, topbar bell, Boards `Convention warnings`, Weekly Focus week segment, and Docs / All Tasks display-only chips. | Closed P1 / PM re-review pending | Calendar `Day / Week / Month` now switches real route state and layout; Month includes grid + agenda while Day/Week show focused agenda panels. Topbar bell opens a no-secret notification/status panel with route links. Boards `Convention warnings` toggles a real board-warning mode. Weekly Focus `Last/This/Next week` updates the route week range. All Tasks and Docs metadata chips are either real filters/reset controls or disabled/read-only with owner-action tooltips. `verify:uiv2-full-fidelity` now checks Calendar actionability and topbar bell wiring. Live Browser checks on `3032/calendar`, `/boards`, `/focus`, `/all`, and `/docs` showed the controls changing state, overflow `0`, and no console warnings/errors. |
| Web-app hardcode and non-interactive control audit | `components.jsx` `Topbar`, `RouteBar`, `FilterBar`, `SettingRow`; `screens-1.jsx` / `screens-2.jsx` / `screens-3.jsx` route action segments; `screens-aux.jsx` state/action guidance | PM/UX asked for a whole-app review of hardcoded UI and buttons that cannot interact. Browser/source audit found stale hardcoded Scope fallback BU labels, static Planner / OKR / Settings segments, a Weekly Focus Owner button with no target, Docs `Rollback hint` with no action, board-card icon actions without explicit intent, selected display-only segments that still looked active, and an async route race where slower route renders could overwrite the next route. | Closed P1 / PM re-review pending | Scope first-paint now says `Loading scope`, app fallback routes to `Configure Scope`, Planner Today/Tomorrow/This week changes the displayed window, OKR All/On track/At risk/Behind filters the objective stack, Settings Audit/retention + Display + Paperclip intake controls are session-stateful, Weekly Focus Owner focuses the owner filter or explains no owner filter is available, Docs rollback hint opens safe guidance, board-card actions expose explicit data/action intent, selected display-only segments are `aria-disabled`, modal/alert fallbacks use SVG icons, and async guards prevent Calendar/Boards/All Tasks/OKR/Weekly Focus stale renders from replacing the active route. Live Browser audit across all 10 desktop routes found `0` enabled visible suspect controls and overflow `0` on every route. |
| Docs / AI Trace route composition still carries legacy docs-browser components | `screens-3.jsx` `ScreenTrace`; `components.jsx` `Trace`; `prototype/css/shell.css` `.trace`; `prototype/css/screens.css` `.trace-row`, `.evidence-card`, `.timeline` | PM/UX live `/docs` review confirmed the route was still not close enough to UI V2. Browser evidence showed the topbar title still read `Docs` instead of `Docs / AI Trace`, the filterbar used production controls (`status: approved`, `link: orphan`, live env, sort dropdown) instead of the prototype chip set (`env: production`, `env: staged`, `status: any`, `agent: any`, `window: 14d`), run ids rendered as raw mono text instead of the `Trace` capsule, and the legacy `Full document detail` disclosure remained visible after the evidence card. | Closed P1 / PM re-review pending | Docs now renders topbar title `Docs / AI Trace`, prototype `ScreenTrace` filter chips, `Trace` capsule run ids with copy affordance, table-first panel, and a vertical evidence/audit card. The legacy document-detail surface is absent from the rendered DOM. `verify:uiv2-full-fidelity` now fails on legacy detail, filter-chip drift, or missing trace capsules. Live `3032/docs` showed overflow `0` and no console warnings/errors. |
| Settings connection edit surface | `screens-3.jsx` `ScreenSettings`; `screens-aux.jsx` `IntegrationRow`, safe-state acceptance notes | PM/UX requested that Connections / Integrations be editable inside the web UI. The previous Settings rows exposed status/action buttons but did not open a route-local editor, so the surface looked clickable without a complete in-page workflow. | Closed P1 / PM re-review pending | Settings now includes a prototype-density Connection editor inside the Integrations panel. Trello, Google Calendar, Google Tasks, Paperclip, and Cloudflare Access rows switch the editor in place; safe fields can be drafted in-session; credential fields are write-only and clear after Save draft. This is UI-only: no API/schema/runtime persistence, live write, or secret display was added. `verify:uiv2-full-fidelity` now fails if the editor, actionable integration rows, or write-only credential inputs disappear. Live `3032/settings` showed editor switching, Save draft feedback, secret clearing/no secret text in body, overflow `0`, and no console warnings/errors. |
| Settings section order, Audit / retention, and Notifications | `screens-3.jsx` `ScreenSettings`; side nav entries `Audit / retention`, `Notifications`, `Display`; `screens-aux.jsx` `SettingRow` | PM/UX found the Settings content order did not match the side nav and the Audit / retention and Notifications UI surfaces were missing. The previous side-nav buttons for Audit, Notifications, and Display also targeted the same Access section, creating a false navigation pattern. | Closed P1 / PM re-review pending | Settings content now follows the source side-nav order: Integrations, Workspace visibility, Hidden boards, BU groups, Paperclip / AI intake, Access & policy, Audit / retention, Notifications, Display. Audit / retention has governance rows for traces, approved-task audit, credential display policy, and trace window; Notifications has route-local toggle controls and session-only draft copy; Display has UI V2 shell/density rows. `verify:uiv2-full-fidelity` now fails on section-order drift, duplicate side-nav targets, missing Audit / retention, or missing notification toggles. Live `3032/settings` showed side targets unique, order correct, notification toggle state change, overflow `0`, and no console warnings/errors. |
| Weekly Focus live-data lane composition and card primitive drift | `screens-3.jsx` `ScreenWeeklyFocus`; `prototype/css/screens.css` `.lanes`, `.lane-item`, `.lt`, `.lm`; Review Queue human-gate contract | PM/UX live `/focus` preview still did not resemble the prototype. Browser evidence showed Do Now and Review AI duplicated the same Trello cards because any card text containing `AI/agent/automation` was treated as Review AI, live counts inflated to `75` / `89`, and each lane item grew to ~200px with production source/status/label chips instead of compact prototype lane items. | Closed P1 / PM re-review pending | Weekly Focus now maps Review AI from actual pending Review Queue proposals only, keeps Do Now / Blocked / Schedule mutually exclusive, renders compact lane items (~85px on live data) with title + board/list/due + label dots, and `verify:uiv2-full-fidelity` fails if lane items exceed 120px or Review AI falls back to Trello production chips. Live `3032/focus` shows overflow `0` and no console warnings/errors. |
| Docs / AI Trace table geometry and duplicate detail surface | `screens-3.jsx` `ScreenTrace`; `prototype/css/screens.css` `.trace-row`, `.evidence-card`, `.timeline` | PM/UX live `/docs` preview still looked visually wrong. Browser geometry found two root causes: the trace table used a stale `padding-bottom: rows * 51px` layout hack, then tablet/controlled QA exposed flex-shrink collapsing the table to ~11px so evidence overlapped the rows. A legacy full document viewer also appeared immediately after the evidence card, mixing the prototype trace/evidence pattern with the older docs-browser pattern in the first viewport. | Closed P0 / PM re-review pending | Table rows now use normal flow with `flex: 0 0 auto`, the rendered route excludes the legacy document-detail surface, evidence keeps visible Source system/Source mode trace copy, and `verify:uiv2-full-fidelity` now fails on table-flow/detail-surface regression. Evidence: live `3032/docs` overflow `0`, no console warnings/errors; regenerated audit reports Docs / AI Trace PASS. |
| Desktop Scope filter interaction | `components.jsx` `Sidebar`, `Topbar`; `prototype/css/shell.css` `.scope-pick`, `.scope-list`, `.nav-item`; production `S.config.groups` board mappings | PM/UX live `/today` preview found Scope rows and the topbar scope picker looked like controls but remained static text. Production routes also kept using all visible cards, so desktop BU scope did not affect Today, All Tasks, Boards, Calendar, Planner, OKR, or Weekly Focus. | Closed P1 / PM re-review pending | Scope rows now render as keyboard/click interactive buttons, the topbar picker updates to the selected BU and clears back to `All BUs`, shared card helpers filter by active group `boardIds`, scoped board helpers feed Boards/Calendar/OKR/Today Quick Add, and the verifier checks sidebar Scope buttons plus topbar Scope interactivity. Live Browser `3032/today` showed Scope click/reset working, `0` overflow, and no API/runtime change. |
| First-paint loading emoji artifact | `screens-aux.jsx` `StateCard`; `components.jsx` icon primitives; prototype loading/state cards avoid emoji fallback icons | PM/UX noted a non-prototype graph emoji during load. The static first-paint `#board-content` still contained an emoji/encoding artifact before JS route bootstrap replaced it. | Closed P2 / PM re-review pending | Static first-paint now uses the UI V2 `loading-box` + spinner primitive, topbar/sidebar first-paint icon slots are SVG-injected, and targeted static scan found no graph/calendar/review/trash/eye/gear emoji fallback in `public/index.html` or `public/app.js`. |
| Desktop filter/action affordance truthfulness | `components.jsx` `FilterBar`, `Topbar`, `RouteBar`; `screens-1.jsx` `ScreenToday`, `ScreenAllTasks`; `screens-2.jsx` `ScreenBoards` | PM/UX noted several filters and buttons still appear actionable without complete behavior. Known examples included display-only scope, route filter chips that do not update global data, and saved-filter/add-filter controls that needed either real behavior or an honest disabled/pending state. | Closed P1 / PM re-review pending | Scope is now wired as real shared filtering. Display-only desktop filter controls that are not wired yet are marked disabled with explanatory titles: Today `Mine/Risky`, All Tasks `List/Group`, `Add filter`, saved view, and Boards `health/owner/stale`. Existing working filters/sorts remain active. |
| Desktop sidebar icons and shell primitive fidelity | `components.jsx` `Ic.*`, `NAV`, `Sidebar`; `prototype/css/shell.css` `.side`, `.side-search`, `.nav-item`, `.nav-item .ico`, `.nav-item .lbl`, `.side-foot` | PM/UX live `/today` preview found non-prototype icon primitives/mapping and sidebar composition drift. Targeted repair now removes emoji fallbacks, maps every Workspace route to prototype `Ic.*` paths at `1.6px` stroke, adds prototype DOM aliases (`side`, `side-head`, `side-logo`, `side-title`, `ico`, `lbl`, `badge`, `side-foot`, `conn-row`), places Settings before Scope, and runs `setupShellPrimitives()` before API bootstrap so icons/search/topbar primitives render on first paint. Later PM/UX review found the prototype footer connector rows duplicated the canonical header telemetry, so production now keeps header status as canonical and uses the sidebar footer for one `Connections` summary plus exception chips only. | Closed P1 / PM re-review pending | Preserve the source-led sidebar contract except for the logged footer UX deviation. `verify:uiv2-full-fidelity` now fails on icon-path drift, primitive-class drift, Settings placement drift, or if the footer returns to mirrored header rows instead of the summary/exception model; live Browser evidence on `3032/boards` showed `Connections`, `2 ok · 1 staged · 1 off`, exception chips for Paperclip/GTasks, legacy footer rows `0`, and overflow `0`. |
| Sidebar label-pill and Scope dot primitive parity | `prototype/css/shell.css` `.nav-item .lbl` plus global `.lbl` atom; `components.jsx` `Sidebar` Scope rows | PM/UX still saw the sidebar as non-prototype. Source review found a prototype CSS interaction: sidebar labels inherit the global `.lbl` atom, so Workspace and Scope labels render as compact uppercase gray label bars, while production had overridden `.nav-item .lbl` into plain title-case text. Scope dots also needed the small prototype bullet rhythm instead of large filled circles. | Closed P1 / PM re-review pending | Sidebar labels now render as 14px uppercase gray label bars, Scope dots use a 6px bullet inside the 16px icon slot, and `verify:uiv2-full-fidelity` fails if these drift. Live `3032/today` check showed label height `14`, uppercase transform, non-transparent label background, 6px Scope dots, and overflow `0`. |
| Topbar scope picker caret primitive | `components.jsx` `Topbar`; `prototype/css/shell.css` `.scope-pick`, `.scope-pick .scope-dot`, `Ic.Down` | Source-led re-sweep found the production topbar scope picker rendered `All BUs` without the prototype down chevron, so the control looked like static status text instead of the prototype dropdown primitive. | Closed P2 / PM re-review pending | `setupShellPrimitives()` now injects the `icon("down")` SVG into `.topbar .scope-pick`, scoped CSS sizes it to 11px, and the verifier checks for dot plus chevron without changing scope behavior. Live `3032/today` check showed dot, chevron, 11px SVG, and overflow `0`. |
| Today Next Up eyebrow time-order parity | `screens-1.jsx` `ScreenToday`; `screens-mobile.jsx` `MToday`; `prototype/css/screens.css` `.next-up .eyebrow`, `.m-eyebrow` | Source-led re-sweep found Today hero eyebrow still used production due wording order (`Next up · Today 18:00`) while the prototype source uses `Next up · 16:00 today`. This was visible in the first meaningful viewport on desktop and mobile. | Closed P2 / PM re-review pending | Added a Today-hero-only due formatter so `Today/Tomorrow/Yesterday HH:mm` becomes `HH:mm today/tomorrow/yesterday` in the Next Up eyebrow while keeping table/card due labels unchanged. Live `3032/today` showed `Next up · 18:00 today`, old order `false`, table due labels preserved, and overflow `0`; `verify:uiv2-full-fidelity` now checks this time order. |
| Mobile shell breakpoint and bottom-tab primitive parity | `screens-mobile.jsx` `MStatusBar`, `MTopbar`, `MTabBar`; `prototype/css/screens.css` `.m-status`, `.m-topbar`, `.m-route`, `.m-tabbar`, `.m-tab`, `.m-tab .ic` | PM/UX live preview in the app pane still felt non-prototype because the old `768px` mobile rules could render a floating rounded bottom nav with a filled active tab, smaller icons, and no phone status row while the source-led `MTabBar` repair only applied at `700px`. This is now repaired through the app-pane breakpoint: status row visible, route title/subtitle stacked, refresh icon preserved, bottom tabbar full-width with transparent active state, 22px icon slots / 18px SVGs, and non-tab routes highlight `More`. | Closed P1 / PM re-review pending | `verify:uiv2-full-fidelity` now includes app-pane 747x910 screenshots and fails on mobile shell drift. Live app-pane Playwright checks for `/today` and `/calendar` showed full-width `0px` radius tabbar, no sidebar, status row visible, no overflow, Today active on `/today`, and More active on `/calendar`. |
| Today desktop routebar/actions/task-row density | `screens-1.jsx` `ScreenToday`; `components.jsx` `Topbar`, `RouteBar`, `Panel`, `TaskRow` | Previously-open Today P1 is now repaired. `Filter` and `Quick add` live in the topbar action slot, the content routebar has no action cluster, Quick add starts collapsed, the extra Today row status column is hidden, task-row metadata uses prototype middle-dot separators, synthesized `No label` text is removed, and desktop topbar hides the route subtitle like prototype `Topbar`. | Closed P1 / PM re-review pending | Preserve the source-led Today contract. `verify:uiv2-full-fidelity` now checks topbar actions, routebar action absence, collapsed quick panel, Reschedule affordance, hidden status column, dot metadata, and no `No label` fallback. Evidence: `compare-today-desktop-1440x900.png`. |
| All Tasks desktop topbar/filter/footer controls | `screens-1.jsx` `ScreenAllTasks`; `components.jsx` `Topbar`, `RouteBar`, `Panel`, `TaskRow` | Previously-open All Tasks P1 is closed in the regenerated screenshot pack. Production now exposes `Open in Trello` and `New task` in the topbar action slot, keeps Export out of the filterbar, places Prev/Next/Export in the table panel footer, and uses prototype group routebar labels. | Closed P1 / PM re-review pending | Preserve the All Tasks topbar/filter/footer contract. `verify:uiv2-full-fidelity` now checks topbar actions, filterbar Export absence, footer Export/pagination, table containment, and `Board - List` header rhythm. Evidence: `compare-all-desktop-1440x900.png`. |
| Weekly Focus lane primitive and density | `screens-3.jsx` `ScreenWeeklyFocus`; `prototype/css/screens.css` `.lanes`, `.lane`, `.lane-head`, `.lane-body`, `.lane-item`, `.lt`, `.lm` | PM/UX noted that many sections still felt like production components. Weekly Focus now uses prototype lane DOM aliases, caps visible lane cards to the prototype rhythm, moves long live-data overflow into `+N more in this lane`, hides legacy lane hints from the desktop first viewport, keeps week navigation in the topbar, and splits lane-item metadata into title / board-list-due / status-label rows. | Closed P1 / PM re-review pending | Preserve the `ScreenWeeklyFocus` lane primitive contract. `verify:uiv2-full-fidelity` now checks topbar week segment, four `lane` columns, lane-head/body primitives, visible card cap, no vertical wrap, and overflow `0`. Evidence: `compare-focus-desktop-1440x900.png`. |
| Global row primitive and Today live-data section density | `components.jsx` `TaskRow`, `BoardTag`, `Due`; `screens-1.jsx` `ScreenToday`; `prototype/css/screens.css` `.trow`, `.board-tag`, `.next-up` | PM/UX reopened that global components and route sections still feel off in live preview. Live Trello data showed long board/list metadata wrapping awkwardly inside Today task rows, and the Today next-up eyebrow/action copy still used production wording (`NEXT UP / DUE WORK`, `Open task`) instead of the prototype rhythm (`Next up · time`, `Open in Trello`). Shared row metadata now truncates instead of wrapping on desktop, mobile row metadata can wrap like `m-trow`, and Today live rows hold a 53px dense rhythm with `Open in Trello` copy. | Closed P1 / PM re-review pending | Live Browser `3032/today` showed row heights `[53,53,53,53,53]`, `Open in Trello`, no old slash copy, overflow `0`, and no console warnings/errors. `verify:uiv2-full-fidelity` passes after regenerating app-pane/mobile evidence. |

## PM/UX Visual Review - 2026-05-17

**Decision:** Targeted shell/sidebar primitive repair is applied and QA-passed. Generated PASS rows remain automated evidence only; PM/UX re-review must confirm the repaired preview before the final visual review returns to accepted state.

| Area | Prototype Source | PM/UX Finding | Severity | Required Action |
| --- | --- | --- | --- | --- |
| Today mobile | `screens-mobile.jsx` `MToday` | Repaired after PM/UX review. Mobile Today now starts after the shell topbar with the dark next-up card, then two stat cards, AI review handoff, and today's work list. Desktop route heading/actions are not ahead of the mobile hero. | Closed P1 / PM accepted | Preserve this mobile-only `MToday` order while keeping live production data. Evidence: `compare-m-today-mobile-390x844.png`. |
| Boards mobile | `screens-mobile.jsx` `MBoards` | Repaired after PM/UX review. Mobile Boards now starts with the two metric cards and board-health cards; desktop filters/search/sort controls are not ahead of the first board set. Board card meta now uses BU group and first available owner from live card data. | Closed P1 / PM accepted | Preserve the `MBoards` first-viewport order and keep secondary controls out of the mobile priority stack. Evidence: `compare-m-boards-mobile-390x844.png`. |
| OKR / Portfolio desktop | `screens-3.jsx` `ScreenOKR` | Repaired after PM/UX review. Desktop OKR now uses the quarter/cycle routebar, All/On track/At risk/Behind segmented control, four-cell stat strip, objective progress blocks, and KR rows. | Closed P1 / PM accepted | Preserve the `ScreenOKR` objective-progress stack and controlled OKR fixture coverage. Evidence: `compare-okr-desktop-1440x900.png`. |
| Mobile More density | `screens-mobile.jsx` `MSettings`, `components.jsx` `NAV`, `data.jsx` `INTEGRATIONS` | Repaired after P1 recovery. Production still exposes Calendar, Planner, OKR / Portfolio, Weekly Focus, Docs / AI Trace, and Settings, but route/integration rows are denser, Google Calendar naming matches prototype source, Trello uses a compact `ok` chip, and the Status card enters the first viewport. | Closed P2 / PM accepted | Preserve route exposure and the compact density. Evidence: `compare-m-more-mobile-390x844.png`. |
| Mobile More helper copy | `screens-mobile.jsx` `MSettings`; `data.jsx` `INTEGRATIONS`; `QC-054` | Second-pass QC found Trello and Cloudflare Access helper lines clipped on mobile-small, making connection status less understandable. | Closed P2 / PM re-review pending | Mobile More integration helper copy now wraps/clamps to two lines, uses Onest instead of mono telemetry styling, and exposes the full helper text via `title`. Targeted `375x667` proof found `5` helper rows, matching title text, horizontal overflow `0`, and console/page errors `0`. Evidence: `settings-mobile-more-helper-wrap-2026-05-18.png`. |
| Mobile shell title stack | `screens-mobile.jsx` `MShell`, `MTopbar`; `prototype/css/screens.css` `.m-topbar .m-route` | Final visual review found production mobile topbar still rendered route title and subtitle on the same row, while prototype stacks the subtitle below the title inside `.m-route`. Shared mobile shell CSS now stacks title/subtitle in column 2 with hamburger in column 1 across Today, Review, Tasks, Boards, and More. | Closed P1 / PM accepted | Preserve the shared mobile topbar grid. Evidence: regenerated mobile compare screenshots and live `3032/settings` title/subtitle geometry check. |

| Route | Source Target | Production Finding | Severity | Target Fix | Evidence / Status |
| --- | --- | --- | --- | --- | --- |
| Settings | `screens-3.jsx` `ScreenSettings`, `components.jsx` `Panel`, `screens-aux.jsx` `IntegrationRow` / `SettingRow` | Side nav, integration rows, Paperclip live controls, Mobile More, in-page Connection editor, section order, Audit / retention, Notifications, and Display were repaired to prototype primitives. Secret values remain write-only/no-display. | Closed P0/P1 | Keep Settings on `set-grid`, `set-side`, `intg-row`, `set-row`, `settings-integration-editor`, and unique side-nav targets; Paperclip live controls stay in a compact sub-panel and connection/notification edits remain session-only until runtime/API scope is explicitly opened. | Pass: `compare-settings-desktop-1440x900.png`, `compare-m-more-mobile-390x844.png`; live `3032/settings` connection editor and notification interaction PASS. |
| All Tasks | `screens-1.jsx` `ScreenAllTasks`, `components.jsx` `TaskTable` | Dense desktop table, prototype filterbar, owner text regression, and mobile task-card row layout were repaired. | Closed P0/P1 | Preserve table/list density and owner/source/next-action metadata while keeping mobile rows scannable. | Pass: `compare-all-desktop-1440x900.png`, `compare-m-tasks-mobile-390x844.png` |
| Review Queue | `screens-1.jsx` `ScreenReview`, `components.jsx` `BulkBar`, `DiffBadge`, `Risk`, `Confidence` | Desktop bulkbar/session/proposal rhythm was tightened; mobile first viewport now prioritizes session/proposal content. Safety controls remain as an accepted deviation. | Closed P1 with deviation | Keep safety extras, diff/risk/confidence, side-effect disclosure, Hold/Edit, and human gate. | Pass with logged deviation: `UI_V2_PROTOTYPE_DEVIATION_LOG.md` |
| Review Queue source-led recovery | `screens-1.jsx` `ScreenReview`, `screens-mobile.jsx` `MReview`, `screens-aux.jsx` empty Review StateCard | Reopened from the handoff because `UI_V2_FULL_ROUTE_FIDELITY_AUDIT.md` reported `mobile approve/reject FAIL` while the route still read PASS, and the live empty `/review` state still used the older `No review sessions yet` pattern instead of the prototype StateCard copy. | Closed P1 | Review mobile primary actions now use the production `.review-mobile-actions` selector in the audit, the audit fails honestly if this regresses, and the live empty Review state now follows the prototype StateCard contract. | Pass: `UI_V2_FULL_ROUTE_FIDELITY_AUDIT.md` now reports `mobile approve/reject PASS`; live `3032/review` empty state verified with 0 overflow. |
| Settings / Mobile More source-led recovery | `screens-mobile.jsx` `MSettings`, `components.jsx` `NAV`, `ScreenSettings` | Live `/settings` on mobile rendered the More route list plus the desktop `Operational control` settings surface in the first viewport, and the More route list omitted `Settings` even though prototype `NAV.slice(4)` includes it. | Closed P1 | Mobile `/settings` now acts as the prototype More surface: Calendar, Planner, OKR / Portfolio, Weekly Focus, Docs / AI Trace, Settings, Integrations, Status. Desktop operational grid is hidden on mobile only; desktop Settings stays unchanged. | Pass: live `3032/settings` mobile check showed 0 overflow, no console warnings/errors, More routes include Settings; `verify:uiv2-full-fidelity` regenerated `compare-m-more-mobile-390x844.png`. |
| MobileShell / Settings integrations source-led recovery | `screens-mobile.jsx` `MStatusBar`, `MTopbar`, `MSettings`; `screens-3.jsx` `ScreenSettings`; `components.jsx` `INTEGRATIONS` | Latest compare showed production mobile shell skipped the prototype phone status row, Settings/Mobile More integration lists omitted the Cloudflare Access operational row, mobile More route badges added non-prototype noise, and the mobile topbar hid the prototype refresh action. | Closed P1 | Shared mobile shell now has a mobile-only phone status row, compact transparent hamburger, and visible refresh action. Settings integration rows align to Trello, Google Calendar, Google Tasks, Paperclip, and Cloudflare Access; More route list keeps prototype route labels without extra badges. Display-only; no auth/runtime behavior changed. | Pass: live `3032/settings` mobile check showed 0 overflow, no console warnings/errors, Cloudflare Access present, and refresh/status row visible; `verify:uiv2-full-fidelity` passed and regenerated mobile evidence. |
| Today | `screens-1.jsx` `ScreenToday`, `screens-mobile.jsx` `MToday`, `components.jsx` `TaskRow`, `RouteBar`, `StatStrip` | Desktop first viewport passes, and mobile was repaired to the prototype `MToday` order after PM/UX review. | Closed P1 / PM accepted | Keep desktop Today intact and preserve the mobile-only next-up/stat/review/work stack. | Pass evidence: `compare-today-desktop-1440x900.png`, `compare-m-today-mobile-390x844.png` |
| Boards Monitor | `screens-2.jsx` `ScreenBoards`, `screens-mobile.jsx` `MBoards`, `components.jsx` board health cards | Desktop card grid passes, and mobile was repaired to the prototype metric-card then board-card order after PM/UX review. | Closed P1 / PM accepted | Keep desktop Boards intact and preserve the mobile board-health first viewport. | Pass evidence: `compare-boards-desktop-1440x900.png`, `compare-m-boards-mobile-390x844.png` |
| OKR / Portfolio | `screens-3.jsx` `ScreenOKR` objective progress blocks and KR rows | Desktop was rebuilt to the prototype objective-progress stack with KR rows after PM/UX review. | Closed P1 / PM accepted | Keep dynamic Trello metadata inside objective cards and preserve safe empty states. | Pass evidence: `compare-okr-desktop-1440x900.png` |
| Weekly Focus | `screens-3.jsx` `ScreenWeeklyFocus` lanes | Visual re-sweep found command/signal/filter panels were ahead of the lane board. Desktop now makes four lanes the primary surface and preserves normal horizontal Review AI text. | Closed P1 | Lanes are first-viewport priority; operating-view extras are not shown ahead of prototype lanes on desktop. | Pass: `compare-focus-desktop-1440x900.png` |
| Docs / AI Trace | `screens-3.jsx` `ScreenDocsTrace`, trace table and inspection panel | Visual re-sweep found production used document cards. Desktop now renders table-first trace rhythm with selected evidence/metadata inspection below. | Closed P1 | Keep table-first trace review; deep document content remains available below or through the selected artifact, but not ahead of trace/audit structure. | Pass: `compare-docs-desktop-1440x900.png` |

## Route Checklist

| Route | Required Components | Parity Status | Deviation |
| --- | --- | --- | --- |
| Today | RouteBar, StatStrip, NextUp, Panel, TaskRow, Review handoff, Calendar peek, Signals | Pass / PM accepted | None |
| Review Queue | RouteBar, BulkBar, SessionCard, ProposalRow, InspectionDrawer, Diff/Risk/Confidence, Timeline | Pass | Safety extras logged |
| All Tasks | RouteBar, FilterBar, TaskTable, TaskRow metadata, StateCard | Pass | None |
| Boards Monitor | RouteBar, StatStrip, FilterBar, BoardCard, Sparkline | Pass / PM accepted | None |
| Calendar | RouteBar, Legend, Calendar grid, Event rows, disconnected state | Pass | Disconnected state reflects controlled fixture |
| Planner | RouteBar, InlineNotice, Source columns, StateCard, Trello due list | Pass | Google Tasks disconnected notice reflects controlled fixture |
| OKR / Portfolio | RouteBar, StatStrip, Objective rows, progress bars, KR rows | Pass / PM accepted | None |
| Weekly Focus | RouteBar, four lanes, Review AI lane, Blocked lane | Pass | None |
| Docs / AI Trace | RouteBar, FilterBar, Trace table, Evidence card, Timeline | Pass | None |
| Settings | RouteBar, set-grid, set-side, IntegrationRow, SettingRow, Paperclip panel | Pass | None |
| Global Edit Card modal | Modal shell, form fields, checklist rows, footer actions | Pass / PM re-review pending | Kept as modal instead of full `TaskDetailDrawer`; deeper drawer conversion would be a separate interaction decision. |

## Visual Re-Sweep Notes

- Review Queue mobile now follows the prototype proposal-card rhythm: session card, compact owner/target/risk/confidence, Reject / Approve first, and secondary safety controls below.
- Review Queue desktop now has a narrower inspection rail and denser proposal rows so the left session list is no longer squeezed.
- Settings desktop now keeps Paperclip runtime controls collapsed behind `Load controls` so the default first viewport matches the prototype operational-control surface and never exposes secrets.
- Mobile More now uses prototype-source route/integration naming and tighter row density while still exposing all non-tab routes; Status is visible in the regenerated first viewport.
- Shared mobile shell now stacks route title and subtitle like `MTopbar`, with the hamburger on the left and refresh action on the right.
- All Tasks mobile includes a hidden-board disclosure when live production data requires it; this is an accepted product-safety data-state difference, not a component-parity blocker.
- Boards Monitor desktop now uses compact board-health cards and hides the extra label-filter rail from the first viewport.
- Weekly Focus desktop now prioritizes the four lane board instead of command/signal/filter cards.
- Docs / AI Trace desktop now uses a trace-table-first layout instead of a card-first document browser.
- Desktop Scope is now a real shared filter instead of static text. It reads production `S.config.groups`, filters route card/board helpers by active group `boardIds`, updates the topbar label, and clears from the topbar picker.
- First-paint loading and visible shell/manage icon fallbacks no longer use emoji glyphs for graph/calendar/trash/eye/gear-style icons. Remaining route/state icons are code-native SVG primitives.
- Desktop filter controls that still lack implementation are now disabled rather than presented as working controls. This keeps the prototype shape visible while avoiding false affordances before the next route-specific filter slice.
- Route header actions now follow the same rule: stateful controls are wired to visible UI state, and metadata-only chips are disabled/read-only with explanatory titles. Calendar Day/Week/Month, Boards Convention warnings, Weekly Focus week navigation, topbar notifications, All Tasks filter affordances, and Docs prototype chips were covered in the latest live Browser pass.
- Review / Docs / Today route-header follow-up closed the remaining weak-action controls: Review Filter now changes visible proposal state, Review Intake policy routes to Settings / Paperclip instead of implying a runtime block, Docs Export creates a safe CSV, and Today Filter highlights the visible work filter surface.
- The black status strip now supports hover/focus descriptions for all 8 telemetry groups. Descriptions stay read-only and safe: no raw runtime values, auth errors, tokens, or hidden environment details.
- Settings button/chip controls now use the same UI V2 primitive rhythm across Integrations, Board Visibility, BU groups, and Monitor labels. The repair is scoped to styling/markup only and keeps existing Settings persistence behavior unchanged.
- Topbar dropdown/action controls now provide visible state and dismissal behavior: Scope opens a listbox-style BU menu, Docs Filter opens a compact filter popover, Bell dismisses competing topbar popovers, and Escape/outside-click closes the surfaces.
- Edit Card on All Tasks now uses scoped UI V2 modal styling while preserving the existing Trello edit flow. This closes the immediate visual mismatch without changing it into a full route-context drawer.
- Global interaction affordance now has a first conservative design-system pass: enabled controls share pointer/hover/focus/active tokens, compact controls gain effective hit-area tokens, Calendar Week exposes pointer and active pressed state, Settings integration buttons expose one active `aria-pressed` state, and the status strip uses one grouped keyboard-focusable `Status details` control instead of eight tiny telemetry tab stops.
- Forced state fixture evidence now covers priority non-populated states without depending on live data: Today loading, Review empty, Review no-match, Review missing-context guard, All Tasks no-match, Docs no-match, Calendar disconnected, and Planner disconnected.

## Acceptance Rules

- A route cannot be called full-fidelity complete while any `P0` remains.
- A route cannot be accepted with `P1` unless it has a PM/UX deviation note.
- Selector presence and no-overflow are supporting checks only.
- Screenshot comparison is evidence; prototype CSS/JS source decides the target shape.
- Latest source-led recovery closed the PM/UX P1/P2 rows for desktop Scope interaction, first-paint emoji artifact, non-wired desktop filter affordances, Today mobile, Boards mobile, OKR / Portfolio desktop, shared mobile shell title stack, sidebar/icon primitive mismatch, Today desktop routebar/task-row drift, All Tasks topbar/footer drift, Weekly Focus lane primitive/density drift, and Edit Card modal design-system drift, plus the Mobile More P2 density row. The RUX visible-copy follow-up restored Review Queue `human approval`, All Tasks `Trello execution source`, Calendar `Connect Google Calendar` / `Google Calendar is disconnected`, Planner `Daily Planner` / `Google Tasks is disconnected`, and Docs `Agent Documents` without changing runtime/API behavior. Automated QA is passing; PM/UX visual re-review is still pending.
- Route-header false-affordance follow-up is also closed for Review / Docs / Today. Automated PASS remains evidence only; PM/UX still decides final visual acceptance.
- `UIV2-FIX-004A` is implemented after PM/UX proceeded with the recommended density/status-strip decisions. Automated PASS remains evidence only; PM/UX must still visually review the interaction feel before final acceptance.

## Latest Verification

- `node --check public/app.js` - Pass.
- `node --check public/js/pages/all-tasks.js` - Pass.
- `node --check public/js/pages/calendar.js` - Pass.
- `node --check public/js/pages/docs.js` - Pass.
- `node --check public/js/pages/review.js` - Pass.
- `node --check public/js/pages/today.js` - Pass.
- `node --check scripts/verify-uiv2-full-fidelity.js` - Pass.
- `node --check scripts/capture-uiv2-state-fixtures.js` - Pass.
- `npm.cmd run verify:uiv2-state-fixtures` - Pass, 8 controlled desktop states under `docs/logs/screenshots/v06-uiv2-state-fixtures/`.
- Full JS syntax sweep for `public/app.js`, `public/js/router.js`, `public/js/utils.js`, `public/js/pages/*.js`, and `scripts/verify-uiv2-full-fidelity.js` - Pass.
- `npm.cmd test` - Pass, 28/28.
- `$env:PORT='3030'; npm.cmd run check:all` - Pass.
- `$env:PORT='3030'; npm.cmd run verify:rux-browser-regression` - Pass, desktop 1440x960 and mobile 390x844 across `/today`, `/review`, `/all`, `/boards`, `/calendar`, `/planner`, `/okr`, `/focus`, `/settings`, `/docs`.
- `$env:PORT='3030'; npm.cmd run verify:uiv2-full-fidelity` - Pass, 10 desktop routes and 5 mobile tabs, with sidebar icon-path, primitive-class, Settings placement, footer connector, desktop topbar, topbar/Sidebar Scope interactivity, Today task-row, All Tasks footer/topbar, and Weekly Focus lane primitive checks.
- Live Browser `http://127.0.0.1:3032/focus` - Pass: four `lane` columns, lane body primitives, visible card cap `[3,3,2,0]`, title-risk `false`, overflow `0`, no console warnings/errors.
- Live Browser `http://127.0.0.1:3032/today` - Pass for desktop shell controls and populated Today: route renders `Daily command center`, Scope row is a button, topbar scope picker has role/tabindex/label/chevron, Scope click changes the topbar label to `Trisilar`, topbar click clears to `All BUs`, targeted emoji graph scan is clean, and overflow is `0`.
- Controlled fixture debug for `/today` - Pass: `Daily command center` rendered after fixing the `quickAddBoards` scope variable root cause; no console/page errors.
- Live Browser `http://127.0.0.1:3032/planner` - Pass: `Daily Planner`, `Google Tasks is disconnected`, Trello deadline panel, overflow `0`, no console warnings/errors.
- Live Browser `http://127.0.0.1:3032/docs` - Pass: `Agent Documents`, trace table first viewport, overflow `0`, no console warnings/errors.
- Live Browser `http://127.0.0.1:3032/calendar` - Pass: connected calendar grid rendered after live Trello/Calendar data finished loading, overflow `0`, no console warnings/errors.
- Live Browser header/action pass on `http://127.0.0.1:3032/calendar`, `/boards`, `/focus`, `/all`, and `/docs` - Pass: Calendar Day/Week/Month changed active state and route layout; topbar bell opened the notification panel; Boards `Convention warnings` became active; Weekly Focus `Last week` changed the week range; All Tasks/Docs readonly chips were disabled with action titles; overflow `0`; no console warnings/errors.
- Live Browser `http://127.0.0.1:3032/all` Edit Card modal - Pass: a live All Tasks row opened the Edit Card surface, the UI V2 scoped modal rendered at `760px` / `8px`, the two-column date row fit inside the panel, checklist loading resolved to 5 rows, checklist text kept normal casing/letter spacing, modal/page overflow was `0`, and console warnings/errors were `0`.
- Live Browser whole-app hardcode/control pass on `http://127.0.0.1:3032` - Pass: Planner window segment changed to `This week`, OKR status segment changed to `At risk`, Settings `Trace export window` / `Desktop density` / `Paperclip intake` segments changed session state, Weekly Focus Owner action focused the owner filter or showed a no-owner explanation, Docs `Rollback hint` showed safe guidance, Calendar/Boards route-race guards kept active route content correct, and the final filtered control audit across Today, Review Queue, All Tasks, Boards Monitor, Calendar, Planner, OKR / Portfolio, Weekly Focus, Docs / AI Trace, and Settings found `0` enabled visible suspect controls with overflow `0` on every route.
- `node --check public/js/pages/boards.js` - Pass.
- `node --check public/js/pages/okr.js` - Pass.
- Live Browser route-header false-affordance follow-up on `http://127.0.0.1:3032/review`, `/today`, and `/docs` - Pass: Review Filter highlights `#review-filterbar`, status/risk chips update proposal state, Intake policy navigates to Settings / Paperclip, Today Filter highlights the visible work filter surface with clear disabled-future-filter feedback, Docs Export downloads a safe visible-row CSV, and overflow is `0`.
- Live Browser status-strip tooltip check on `http://127.0.0.1:3032/today` - Pass: 8 status groups expose safe descriptions, Trello status focus shows the custom tooltip, unsafe description hits `[]`, overflow `0`, and console warnings/errors `0`.
- Live Browser Settings button/chip typography check on `http://127.0.0.1:3032/settings` - Pass: Edit groups, Manage, Visible, Delete, board chips, and monitor label remove controls use Onest, compact design-system heights/radius, action icons where appropriate, `monospaceButtons: []`, overflow clean, and console warnings/errors `0`.
- Desktop Playwright topbar dropdown/action check on `http://127.0.0.1:3032/docs` at `1440x900` - Pass: Docs Filter opens a dialog popover with focused search, applying `status: approved` closes the menu and shows active count `1`, Clear restores the default state, Scope opens a listbox popover with All BUs and configured BU rows, Bell opens a notification popover and dismisses the Scope menu, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/topbar-docs-filter-popover-1440x900.png`.
- Live Browser Docs trace summary / reader separation check on `http://127.0.0.1:3032/docs` - Pass: route evidence card shows `Selected trace`, `Trace summary`, `Audit preview`, and `Reader holds full detail`; summary card does not contain `Full document content`; `Open reader` opens the drawer with full Markdown, linked task, evidence, and audit timeline; clipped side-card count `0`, overflow `0`, and console warnings/errors `0`. Screenshots: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-trace-summary-separated-2026-05-18.png` and `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-summary-reader-separation-2026-05-18.png`.
- Live Browser Docs reader content craft / select-first row check on `http://127.0.0.1:3032/docs` - Pass: clicking the second trace row selects `Weekly Marketing Sync Summary`, sets `aria-pressed=true`, keeps the reader closed, updates the summary card, and exposes `Open reader`; clicking `Open reader` opens the reader for the selected trace. The reader shows the dedicated document surface, bordered Markdown card, safety callout, evidence card, clipped side-card count `0`, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-reader-professional-polish-weekly-2026-05-18.png`.
- Live Browser Review Queue duplicate refresh check on `http://127.0.0.1:3032/review` - Pass: route title `Review Queue`, route actions text `Filter`, global refresh visible as the single refresh control, route refresh absent, Filter focuses/highlights `#review-filterbar`, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/review-topbar-refresh-dedup-2026-05-18.png`.
- Live Browser R154 closeout check on `http://127.0.0.1:3032/docs` - Pass: route title `Docs / AI Trace`, visible Docs content, trace rows present, one `Open reader` CTA, reader drawer opened with full document/evidence/linked-task/audit surfaces, Close returned to the route, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/docs-live-r154-reader-qa-2026-05-18.png`.
- Live Browser Settings side-nav anchor check on `http://127.0.0.1:3032/settings` - Pass: `Audit / retention` and `Display` side-nav clicks set `aria-current=true` on the clicked item immediately, settle target sections near `90px` below the topbar, focus the target section ids, keep overflow `0`, and produce console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/settings-side-nav-display-anchor-2026-05-18.png`.
- Live Browser Planner source-column check on `http://127.0.0.1:3032/planner` - Pass: route title `Planner`, `Daily Planner`, Google Tasks disconnected surface, Trello Deadlines panel, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/planner-source-columns-desktop-2026-05-18.png`.
- Live Browser Today row action discoverability check on `http://127.0.0.1:3032/today` - Pass: 8 visible `Open` row actions, first action `68x28`, task-specific accessible label/title, focus-within row cue, Edit Card opens for the selected task, modal closes cleanly, overflow `0`, and console warnings/errors `0`. Screenshots: `docs/logs/screenshots/v06-uiv2-full-fidelity/today-row-focus-open-action-2026-05-18.png` and `docs/logs/screenshots/v06-uiv2-full-fidelity/today-row-hover-actions-2026-05-18.png`.
- Live Browser Settings board visibility controls check on `http://127.0.0.1:3032/settings` - Pass: 9 visible board visibility buttons, contextual `aria-label`/`title`, Onest typography, compact UI V2 `78x26` geometry, green visible-state styling, overflow `0`, and console warnings/errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/settings-board-visibility-controls-2026-05-18.png`.
- Targeted Playwright Mobile More helper-copy check on `http://127.0.0.1:3032/settings` at `375x667` - Pass: `.settings-mobile-more` visible, `5` integration helper rows, helper text uses Onest, `white-space` is not `nowrap`, `-webkit-line-clamp` is `2`, full text is preserved in `title`, horizontal overflow `0`, and console/page errors `0`. Screenshot: `docs/logs/screenshots/v06-uiv2-full-fidelity/settings-mobile-more-helper-wrap-2026-05-18.png`.
- Screenshot evidence manifest - Added `docs/logs/screenshots/v06-uiv2-full-fidelity/SCREENSHOT_MANIFEST.md` for `QC-009`, separating baseline route comparisons, targeted desktop evidence, forced state fixtures, interaction-matrix captures, and whole-system QC outputs. Automated screenshots remain supporting evidence only; PM/UX visual review is still the final gate.
- Desktop control help audit - Added `docs/logs/screenshots/v06-uiv2-full-fidelity/CONTROL_HELP_AUDIT_2026-05-18.md` for `QC-004`; visible desktop icon/status-like/compact controls across 10 routes had missing help candidates `0`, overflow `0`, and existing interaction-matrix screenshots cover Status details, Scope, and Docs Filter help surfaces.
- Mobile nav detector reconciliation - `QC-037` and `QC-057` are now PM/UX review pending because `verify:uiv2-full-fidelity` already uses `.mobile-route-item .nav-label`, enforces exact labels `Today / Review / Tasks / Boards / More`, emits `production-mobile-nav-label-check.png`, and checks Mobile More route exposure through `.settings-mobile-more`.
- Stable interaction-locator reconciliation - `QC-060` is now PM/UX review pending because `capture-uiv2-interaction-matrix.js` uses stable ids/classes/data selectors for high-risk controls instead of broad text-regex locators that can confuse Scope and route Filter controls.
- QC selector contract - `QC-038` is now PM/UX review pending because `qc-selector-contract.md` documents the stable selector rule for future sweeps: Scope, Docs Filter, Today Filter, Review Filter, mobile nav, toast, status details, All Tasks sort, and mobile route bar must use direct selectors before a product UI bug is opened.
- Mobile bottom-nav keyboard semantics - `QC-073` actionable portion is now PM/UX review pending: bottom-nav items are semantic `button[type=button]` controls with route-specific `aria-label`s and verifier coverage; app-pane/mobile probe confirmed keyboard reachability, settled route titles, overflow `0`, and no console warnings/errors. Residual full-cycle `BODY` wrap is logged rather than hidden.
- Interaction-matrix expansion - `QC-039` is now evidence-expanded for desktop PM/UX review: `capture-uiv2-interaction-matrix.js` covers 19 high-risk controls and 75 default/hover/focus/click/open screenshots across Status details, Scope, Bell, Today, Review, Docs, All Tasks, Boards, Planner, Calendar, OKR, Weekly Focus, and Settings. Selector misses `0`, console/page errors `0`, overflow `0`, missing button types `0`, and enabled controls with default cursor `0`. Live Browser `/docs` popover sanity also passed. Residual Manual Upload Escape/focus behavior remains logged under focus-recovery policy rather than hidden.
- Hidden-focusable classifier - `QC-077` now has a classifier artifact instead of raw-total-only escalation. `hidden-focusable-classification.md` groups the fourth-pass `1880` raw signals into confirmed zero-rect visible-tree `369`, modal/drawer review `988`, legacy shell/statusbar or superseded-status review `250`, responsive hidden-control review `131`, likely false/inert `6`, tooling-only hidden-state `136`, and opacity-zero confirmed risk `0`. This is evidence triage, not final acceptance.
- Absolute/fixed element classifier - `QC-089` now has a classifier artifact. `absolute-element-classification.md` groups all fourth-pass positioned signals as expected shell primitives: sidebar search adornments `20`, mobile bottom nav `20`, Settings sticky side nav `1`, with high-z unclassified fixed surfaces `0` and unclassified positioned surfaces `0`.
- Contrast/focus heuristic classifier - `QC-075` now has a classifier artifact. `contrast-focus-heuristic-classification.md` groups third-pass low-contrast signals `79` into alpha-background compositing `5`, dense table/header token review `24`, mobile section label review `2`, eyebrow muted token review `48`, and unclassified low contrast `0`; it also categorizes small target signals `478` and generic-control signals `127` so future QA does not treat broad heuristics as automatic blocker failures.
- Whole-system UI/UX micro-QC sweep on `http://127.0.0.1:3032` - Complete: 10 routes across desktop `1440x900`, laptop `1366x768`, tablet `1024x768`, app-pane `747x910`, mobile `390x844`, and mobile-small `375x667`; navigation errors `0`, console/page errors `0`, horizontal overflow over `2px` `0`, emoji-like fallback glyphs `0`. Remaining UX suspects are now tracked in `UI_V2_WHOLE_SYSTEM_UX_QC_LEDGER.md` with 40 route/section/control/state rows and evidence in `docs/logs/screenshots/v06-uiv2-whole-system-qc/`.
- Second-pass UI/UX micro-QC sweep on `http://127.0.0.1:3032` - Complete: 10 routes across desktop `1440x900`, app-pane `747x910`, and mobile-small `375x667`; console/page errors `0`, horizontal overflow over `2px` `0`, active/small target route-viewport hits `28`, clipping/readability route-viewport hits `27`, and focus-style route-viewport hits `10`. New actionable rows QC-041 through QC-060 were added to `UI_V2_WHOLE_SYSTEM_UX_QC_LEDGER.md`, led by Docs mobile filterbar/table overlap and Calendar mobile event-chip readability. Evidence: `docs/logs/screenshots/v06-uiv2-whole-system-qc/qc-second-pass-results.json`, `qc-second-pass-summary.md`, and `qc2-*.png`.
- Third-pass UI/UX micro-QC sweep on `http://127.0.0.1:3032` - Complete: in-app Browser `/docs` current-tab check passed with meaningful content and console warn/error count `0`; Playwright batch covered 10 routes across desktop `1440x900`, app-pane `747x910`, and mobile-small `375x667`; console/page errors `0`; horizontal overflow over `2px` `0`; 12 keyboard-focus sequences and 10 interaction probes captured. New rows QC-061 through QC-075 were added for status-strip tab cost, search input labels/focus rings, hidden legacy modals, All Tasks edit affordance, repeated generic action labels, low-contrast muted labels, route heading semantics, desktop Docs trace clipping, and QC tooling limits. Evidence: `docs/logs/screenshots/v06-uiv2-whole-system-qc/qc-third-pass-results.json`, `qc-third-pass-summary.md`, `qc3-hidden-modal-inspection.json`, and `qc3-*.png`.
- Fourth-pass UI/UX micro-detail QC sweep on `http://127.0.0.1:3032` - Complete: Playwright batch covered 10 routes across desktop `1440x900`, app-pane `747x910`, and mobile-small `375x667`; console/page errors `0`; horizontal overflow over `2px` `0`; 5 interaction probes captured. New rows QC-076 through QC-092 were added and `UI_V2_WHOLE_SYSTEM_UX_QC_FIX_DETAIL_APPENDIX.md` explains fix families for hidden zero-size controls, focus return, scope semantics, readonly chips, All Tasks sort state, button type/cursor/hit areas, clipping policy, Docs mobile audit layout, Calendar readability, route headings, status-strip help, and QC tooling accuracy. Evidence: `docs/logs/screenshots/v06-uiv2-whole-system-qc/qc-fourth-pass-results.json`, `qc-fourth-pass-summary.md`, and `qc4-*.png`.
- User-friendly improvement opportunity ledger - Complete: `UI_V2_USER_FRIENDLY_IMPROVEMENT_OPPORTUNITY_LEDGER.md` records 60 non-blocking improvement options across global shell, route workflows, data readability, responsive ergonomics, Settings/integrations, and QA process. It separates defects from user-friendly enhancements and marks impact, effort, scope, timing, related QC rows, and PM decision needs. No product code/runtime change was made.
- `$env:PORT='3030'; npm.cmd run verify:uiv2-full-fidelity` - Pass after hardcode/control recovery; regenerated `UI_V2_FULL_ROUTE_FIDELITY_AUDIT.md`, `UI_V2_COMPONENT_PARITY_AUDIT.md`, and screenshot evidence.
- `$env:PORT='3030'; npm.cmd run verify:uiv2-full-fidelity` - Pass after Review filterbar / Intake policy verifier update; all 10 desktop routes and 5 mobile tabs passed.
- `$env:PORT='3030'; npm.cmd run verify:uiv2-full-fidelity` - Pass after adding status-strip description checks; all 10 desktop routes and 5 mobile tabs passed.
- `$env:UIV2_APP_BASE_URL='http://127.0.0.1:3032'; node scripts\capture-uiv2-interaction-matrix.js` - Pass after `UIV2-FIX-004A`; statusbar focusable groups `0`, status details ready `true`, enabled controls with default cursor `{}`, overflow `0`, console/page errors `0`.
- In-app Browser spot-check on `http://127.0.0.1:3032/today`, `/calendar`, and `/settings` - Pass: status details focus returns to itself after Escape, Calendar Week cursor is `pointer` and active toast appears, Settings Trello integration is active with `aria-pressed=true`, overflow `0`, console warnings/errors `0`.
- `git diff --check` - Pass with Windows CRLF warnings only.
- `rg "^(<<<<<<<|=======|>>>>>>>)"` - No conflict markers.
